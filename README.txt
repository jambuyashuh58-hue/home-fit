# HomeFit – Static Zip (Fix 404 on Vercel)

1) Unzip and push the **index.html** to the **repo root**.
2) In Vercel Project → Settings → Framework Preset = **Other** (Static Site).
3) Ensure Root Directory (if set) points to the folder that contains this `index.html`.
4) Commit & push to redeploy.
